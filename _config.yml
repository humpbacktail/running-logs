title: MASA Running Logs
theme: minima
url: "https://humpbacktail.github.io"
baseurl: "/running-logs"

collections:
  logs:
    output: true
    permalink: /logs/:name.html

include:
  - stats.json
  
exclude:
  - logs/template.md.tpl
  - images   # 画像はraw.githubusercontent配信に移行（Pages 1GB上限回避・2026-08-08）

defaults:
  - scope:
      type: logs
      path: ""
    values:
      layout: page
      nav_exclude: true

  - scope:
      type: pages
      path: ""
    values:
      layout: page

render_with_liquid: false
